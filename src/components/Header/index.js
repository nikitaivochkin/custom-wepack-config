import React from 'react';
import Navigation from '../_navigation';
import './header.sass';

const Header = () => (
  <div className="header component">
    <div className="header__container component__container">
      <Navigation />
    </div>
  </div>
);

export default Header;
